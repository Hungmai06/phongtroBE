spring:
  application:
    name: room
  datasource:
    url: jdbc:postgresql://localhost:5432/room
    username: admin
    password: Admin123@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create | create-drop | update | validate | none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: maivanhung0604@gmail.com
    password: jsxk zdgp noqo mhrr
    protocol: smtp
    default-encoding: UTF-8
    test-connection: false
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
  data:
    elasticsearch:
      repositories:
        enabled: true
  elasticsearch:
      uris: http://localhost:9200

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

openapi:
  service:
    server: http://localhost:8080
    title: My API Service
    version: 1.0.0

jwt:
  expiryMinute: ${JWT_EXPIRY_MINUTE:5}
  expiryDay: ${JWT_EXPIRY_DAY:14}
  accessKey: ${JWT_ACCESS_KEY:Z3YmEphCpEaLM14gjwN3WWMsNkUJoncebqs6ZIElS0M=}
  refreshKey: ${JWT_REFRESH_KEY:YzWtBjqYoRn5XX+AUkf5iSXGazL5U2LJF3cjR1Ruc3k=}

servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

app:
  booking:
    expiration-hours: 24

